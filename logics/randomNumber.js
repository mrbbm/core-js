function getRandomNumber(min, max) {
    try {
        // Math.random() * (max - min)
        let result = Math.floorfloor(1.52)
        console.log(`Random Numbers between ${min} and ${max} is ${result}`);   
    } catch (error) {
        console.log(error);
    }
}

